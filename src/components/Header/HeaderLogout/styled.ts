import styled from 'styled-components';

export const StDoor = styled.img`
  display: block;
  width: 23px;
  height: 23px;
  cursor: pointer;
  &:hover {
    filter: drop-shadow(0 0 2px #ddd);
  }
`;
